
  - Functions: Extract functions have changed?
  - Functions: any unused functions? (f.e. flatten)
  - NormaliseUtils now has lots of unused code.
  - Add static factory methods to ListFuncs, ArrayFuncs, etc

  - Create mapIfNotNull and mapMapIfNotNull
  - Reorganize functions in package org.op4j.functions
  - Functions in StringFuncs: do they have to be nullasnull or notnull?
    
    
    
----------------    
Math. Operations
----------------

Target: list of numbers
	max()  
	min() 
	sum()
	avg(), avg(RoundingMode rm), avg(MathContext mc)
		
Target: a number
	round(RoundingMode rm), round(MathContext mc)
	abs()
	
	add(x)
	sub(x) 
	div(x), div(x, RoundingMode rm), div(x, MathContext mc)
	mod(x)
	mul(x)
	pow(x), pow(x, MathContext mc)
	
Add javadoc

----------------